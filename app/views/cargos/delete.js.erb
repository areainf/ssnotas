<% #Llamado cuando se crea un cargo con ajax
#Simplemente informar el resultado del metodo create
%>


<% if $ok %>
   $("#cargo_<%= @cargo.id %>").fadeOut("slow",new function(){$("#cargo_<%= @cargo.id %>").remove();});

<%
   else
%>
$('body').append("<div id='tempmsj'></div>");
$('#tempmsj').dialog({
                        autoOpen: false,
                        resizable: false,
                        show: 'fade',
                        hide: 'fade',
                        height: 140,
                        modal: true,
                        title: 'No se pudo eliminar',
                        close: function (event, ui) { $('#tempmsj').remove();},
                        open: function (event, ui) {
                          $('#tempmsj').html('<p>Existen personas con este cargo</p>');
                        }
                    }).dialog("open");

setTimeout(function () { $('#tempmsj').dialog("close"); }, 2000);

<% end %>
