<%= helper_search_field({:input_id => 'nota_temporal_index_field_search', :grilla =>'table-notas_temporales' },
{:controller => 'notas_temporales', :action=>'search'}) %>

<script language="JavaScript">
   $(function() {
    /*ASOCIAR EL TR  A EVENTOS
      Al hacer click en cualquier fila de la tabla nos lleva
      al methodo show de la nota-recibida */
    $('.nota-temporal-tr').click(function() {
        field_id=$(this).attr('id');
        id = field_id.replace("nota_temporal_","");
        dir = '<%=  url_for :controller => 'notas_temporales', :action => 'show', :id => "reemplazar" %>';
        url = dir.replace("reemplazar",id);
        window.location=url;

    });
  });


</script>