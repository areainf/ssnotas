<% #Llamado cuando se crea un estado_nota con ajax
#Simplemente informar el resultado del metodo create
%>


<% if $ok %>
   $("#estado_nota_<%= @estado_nota.id %>").fadeOut("slow",new function(){$("#estado_nota_<%= @estado_nota.id %>").remove();});

<%
   else
%>
$('body').append("<div id='tempmsj'></div>");
$('#tempmsj').dialog({
                        autoOpen: false,
                        resizable: false,
                        show: 'fade',
                        hide: 'fade',
                        height: 140,
                        modal: true,
                        title: 'No se pudo eliminar',
                        close: function (event, ui) { $('#tempmsj').remove();},
                        open: function (event, ui) {
                          $('#tempmsj').html('<p>Hay registros que dependen de este estado_</p>');
                        }
                    }).dialog("open");

setTimeout(function () { $('#tempmsj').dialog("close"); }, 2000);

<% end %>
