/*
 * Se utiliza para mostrar el template asociacion luego de que se elimino o creo una asociacion
 * mediante el formulario asosciacion
*/

$('#div_asociacion').html('<%= escape_javascript(render("asociar")) %>');