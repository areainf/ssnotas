<% #Llamado cuando se crea un persona con ajax
#Simplemente informar el resultado del metodo create
%>


<% if $ok %>
   $("#persona_<%= @persona.id %>").fadeOut("slow",new function(){$("#persona_<%= @persona.id %>").remove();});

<%
   else
%>
$('body').append("<div id='tempmsj'></div>");
$('#tempmsj').dialog({
                        autoOpen: false,
                        resizable: false,
                        show: 'fade',
                        hide: 'fade',
                        height: 140,
                        modal: true,
                        title: 'No se pudo eliminar',
                        close: function (event, ui) { $('#tempmsj').remove();},
                        open: function (event, ui) {
                          $('#tempmsj').html('<p>Esta persona tiene registros asociados</p>');
                        }
                    }).dialog("open");

setTimeout(function () { $('#tempmsj').dialog("close"); }, 2000);

<% end %>
