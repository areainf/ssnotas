<!-- Llamado por search cuando se realiza una busqueda con ajax, -->
$('.paginaremota').html("<%= escape_javascript(render("row_index"))%>");

<!--#LLamado cuando se realiza la paginacion por ajax -->